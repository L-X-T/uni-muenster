import { AsyncFlightValidatorDirective } from './async-flight-validator.directive';

describe('AsyncFlightValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncFlightValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
