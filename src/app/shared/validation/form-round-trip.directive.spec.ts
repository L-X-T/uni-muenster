import { FormRoundTripDirective } from './form-round-trip.directive';

describe('FormRoundTripDirective', () => {
  it('should create an instance', () => {
    const directive = new FormRoundTripDirective();
    expect(directive).toBeTruthy();
  });
});
